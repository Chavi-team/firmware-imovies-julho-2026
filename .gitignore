bin/
tools/.venv-bancada/
tools/.bancada.json
tools/bancada-live.log
__pycache__/

# pacotes de bancada (buildados localmente)
packaging/dist/
packaging/build/
packaging/.venv-build/
packaging/avrdude/
# esptool standalone (ferramenta) — não usamos binário externo (esptool é módulo
# pip, chamado em-processo). Ignorado caso alguém queira embarcar um standalone.
packaging/esptool/
# NOTA: packaging/firmware-ci/*.bin (firmware da CI) É VERSIONADO — por decisão do
# Leonardo (09/09), no repo PRIVADO, para o build Windows tê-lo. Contém o
# OTA_TOKEN (segredo de baixa sensibilidade, já embutido em toda CI). Se um dia
# for preciso rotacionar o token, regerar os bins e trocar no core.
.DS_Store
**/.DS_Store

# harness local (settings + worktrees)
.claude/
